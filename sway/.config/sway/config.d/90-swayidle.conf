### Idle configuration (power-aware)
# On AC:      no idle actions (no lock, no display off, touchbar stays bright)
# On battery: lock after 5min, display off after 10min

exec ~/.config/sway/scripts/power-idle.sh
